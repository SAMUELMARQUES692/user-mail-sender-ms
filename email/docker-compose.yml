version: '3.8'

services:
  postgres:
    image: postgres:16
    container_name: ms-email-db
    environment:
      POSTGRES_DB: ms_email_ms
      POSTGRES_USER: ${DATABASE_USERNAME_EMAIL}
      POSTGRES_PASSWORD: ${DATABASE_PASSWORD_EMAIL}
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: